<h1>All Users</h1>
<table>
<% @users.each do |user| %>
  <tr>
    <td><img src=<%= user.image %>></td>
    <td><%= link_to (user.fname + " " + user.lname), user %></td>
    <td><%= user.location %></td>
    <td><%= user.description %></td>
  </tr>
<% end %>
</table>
